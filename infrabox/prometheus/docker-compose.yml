version: '3'

services:
  prometheus:
    image: quay.io/prometheus/prometheus:v2.45.9
    restart: always
    volumes:
      - "prom_data:/prometheus"
      - "./prometheus.yml:/etc/prometheus/prometheus.yml:ro"
    ports:
      - "9090:9090"